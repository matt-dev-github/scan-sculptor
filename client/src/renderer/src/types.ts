export interface Device {
    hostname: string;
    ip_address: string;
    mac_address: string;
    operating_system: string;
    device_type: string;
    network_segment: string;
    response_time: string;
}